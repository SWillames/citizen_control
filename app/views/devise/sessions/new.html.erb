<h5 class="card-title text-center ">Bem vindo de Volta!</h5>
<div class="text-center"><%= image_tag "logo", size: "45x45", class: "img-thumbnail"%></div>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class="form-label-group">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "   " %>
    <%= f.label :email %>
  </div>

  <div class="form-label-group">
    <%= f.password_field :password, autofocus: true, autocomplete: "current-password", class: "form-control", placeholder: "  " %>
    <%= f.label :password %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="form-label-group">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Entrar", class: "btn btn-lg btn-primary btn-block " %>
  </div>
<% end %>
Ainda nao possui conta cadastre-se
<%= render "devise/shared/links" %>